version: 2
jobs:
  build:
    working_directory: ~/pgjwt
    machine: true
    # docker:
    #   - image: cimg/node:12.16
    steps:
        - checkout      
        # - setup_remote_docker
            # docker_layer_caching: true                    
        - run:
            name: Build + Run Tests
            command: ./test.sh
            

